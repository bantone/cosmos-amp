# Core model dependencies
torch==2.3.1
transformers>=4.44.0
accelerate>=0.33.0
huggingface_hub>=0.24.0
toml

# Media processing
opencv-python>=4.11.0
Pillow>=10.0.0

# Pinned below 2.0 — pyarrow, numba, scipy in Anaconda base are compiled against NumPy 1.x
numpy<2

# Application
streamlit>=1.30.0
